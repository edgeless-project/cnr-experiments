# 007-ced21-migration

## Scenario

5 nodes with `RUST_WASM` and `CONTAINER` run-times.

## Repeatability

Requirements:

- WASM functions: `double.wasm`, `trigger.wasm`

## Dataset

The datasets can be downloaded with:

```shell
../../scripts/download-artifacts.sh
```

If needed, the required Python packages can be installed with `pip install -r requirements`.
